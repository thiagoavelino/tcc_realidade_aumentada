package Test;

import java.awt.image.BufferedImage;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import Model.LabelArea;
import Model.Pixel;

public class UnitTestsLabelArea {
	private LabelArea labelArea;

	@Before
	public void setUp() throws Exception {
		labelArea = new LabelArea();
	}

	@Test
	public void testAddPixelAreaTest() {
		Pixel pix = new Pixel(0,0);
		labelArea.addPixelArea(pix);
		Assert.assertSame(labelArea.getPixelsArea().get(0), pix);
		//this.imgAlgorithms.transformBw();
	}
	
	@Test
	public void automatedThreshold() {
		//this.imgAlgorithms.automatedThreshold();
	}

}
